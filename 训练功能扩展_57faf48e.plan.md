# Price Action 训练器 — 训练功能扩展使用说明书

---

## 目录

1. [功能总览](#1-功能总览)
2. [训练前：计划卡](#2-训练前计划卡)
3. [模拟交易训练](#3-模拟交易训练)
   - 3.1 风险仓位管理
   - 3.2 滑点与手续费
   - 3.3 市价与限价下单
   - 3.4 交易执行复盘弹窗
4. [PA / 方向训练](#4-pa--方向训练)
   - 4.1 方向预测
   - 4.2 结构标注保存
   - 4.3 剧本训练
5. [场景训练](#5-场景训练)
6. [结束训练与自动评分](#6-结束训练与自动评分)
7. [复盘面板](#7-复盘面板)
   - 7.1 总览
   - 7.2 交易日志
   - 7.3 训练记录
   - 7.4 错题本
8. [典型训练流程](#8-典型训练流程)
9. [数据结构说明](#9-数据结构说明)
10. [常见问题](#10-常见问题)

---

## 1. 功能总览

本次扩展从 **交易训练** 和 **PA (Price Action) 训练** 两个维度对程序进行增强，核心新增能力如下：

| 模块 | 新增能力 |
|------|---------|
| 训练前计划 | 计划卡弹窗：Setup、场景、方向、失效条件、难度、风险、剧本 |
| 交易训练 | 风险仓位自动计算、滑点模拟、手续费模拟、执行评分、错误分类 |
| PA 训练 | 图表结构标注持久化、剧本推演、方向预测 |
| 场景筛选 | 6 种预设场景模板，智能筛选训练片段 |
| 复盘面板 | 4 个标签页：总览 / 交易日志 / 训练记录 / 错题本 |
| 错题本 | 自动生成、分类标记、复训追踪 |
| 统计钻取 | 按 Setup / 错误类型 / 场景 / 出场方式分组统计 |

---

## 2. 训练前：计划卡

点击 **「开始训练」** 按钮后，系统会弹出 **训练前计划卡** 对话框，要求你在训练开始前做好书面计划。

### 计划卡字段说明

| 字段 | 说明 | 适用模式 |
|------|------|---------|
| **Setup** | 本次训练聚焦的交易形态。可选项：趋势回踩、区间突破、假突破反手、反转确认、供需区、自定义 | 交易 + PA |
| **场景模板** | 系统会尝试筛选符合该场景特征的 K 线片段（详见第 5 节）。选「随机」则不过滤 | 交易 + PA |
| **预期方向** | 你对未来走势的预判：看多、看空、震荡、未设定 | 交易 + PA |
| **失效条件** | 在什么情况下你认为计划失效（例如"跌破前低""收回区间"） | 交易 + PA |
| **难度 (1-5)** | 你对本次行情难度的主观评估，1 为最简单，5 为最难 | 交易 + PA |
| **单笔风险 %** | 每笔交易愿意承受的资金损失比例（默认 1%），仅在交易模式下可编辑 | 交易 |
| **计划 / 剧本** | 自由文本。交易模式写入场逻辑、失效条件、预期出场；PA 模式写剧本推演 | 交易 + PA |

### 使用要点

- 填写计划卡是 **强制步骤**，点击「取消」会放弃本次训练。
- 计划卡的信息会保存到训练记录中，供复盘时回顾。
- 训练面板右侧的「本轮计划」区域会实时显示你填写的计划摘要。

---

## 3. 模拟交易训练

选择 **「模拟交易」** 模式后，训练面板的交易操作区会展示全部交易控件。

### 3.1 风险仓位管理

系统新增了 **基于风险百分比自动计算仓位** 的能力。

**操作方式：**

1. 在「风险%」栏输入你每笔交易愿意亏损的资金百分比（如 `1.00` 表示 1%）。
2. 在「止损」栏输入止损价格。
3. 有两种使用方式：
   - **自动模式（推荐）**：勾选「自动按风险算仓位」复选框。此时做多/做空按钮点击后会自动计算并填入数量。
   - **手动模式**：取消勾选后，点击「计算仓位」按钮手动计算，数量会回填到「数量」输入框。

**计算公式：**

```
风险金额 = 当前资金 × 风险百分比
每股风险 = |入场价格 - 止损价格|
仓位数量 = 风险金额 ÷ 每股风险（取整到 100 股的整数倍，最少 100 股）
```

**注意：** 启用自动仓位时必须先设置止损，否则系统会弹出提示。

### 3.2 滑点与手续费

为了更真实地模拟实盘环境，系统会自动应用：

- **滑点模拟**：默认 0.05%。买入时价格略上调，卖出时价格略下调，模拟实际成交偏差。
- **手续费计算**：遵循 A 股规则（万三佣金 + 千一印花税卖出），在交易统计中单独列出。

交易统计中的「手续费」栏会显示累计手续费，「资金」栏显示扣除手续费后的剩余资金。

### 3.3 市价与限价下单

| 操作 | 按钮 | 说明 |
|------|------|------|
| 市价做多 | 红色「市价做多」 | 以当前 K 线收盘价 + 滑点买入 |
| 市价做空 | 绿色「市价做空」 | 以当前 K 线收盘价 - 滑点卖出（A 股默认禁用） |
| 限价买入 | 「限价买入」 | 在图表上生成一条可拖动价格线，K 线触及时自动成交 |
| 限价卖出 | 「限价卖出」 | 在图表上生成一条可拖动价格线，K 线触及时自动成交 |
| 撤单 | 「撤单」 | 取消当前挂出的限价委托 |
| 平仓 | 「平仓」 | 以当前价格手动平仓 |

**限价委托注意事项：**
- 限价买入默认价格为当前收盘价的 99%，限价卖出为 101%。
- 你可以在图表上 **拖动价格线** 来调整委托价格。
- 限价委托挂出后，每次 K 线推进时系统会检查是否成交。

### 3.4 交易执行复盘弹窗

每笔交易平仓后（包括手动平仓、止损、止盈、训练结束时强制平仓），系统会弹出 **交易执行复盘** 对话框。

**复盘弹窗包含：**

| 字段 | 说明 |
|------|------|
| 交易摘要 | 显示做多/做空、入场价→出场价、盈亏、出场原因 |
| 错误分类 | 多选勾选框。可选项：追涨杀跌、过早止盈、拖延止损、计划外交易、错过入场、无效加仓、仓位过大、结构误判 |
| 执行评分 | 0-100 分，步进 5，默认 70。反映你对本次执行质量的主观评价 |
| 入场理由 | 描述为什么入场，是否在计划内 |
| 出场复盘 | 是否按计划出场，出场后有什么反思 |

**使用要点：**
- 点击「确定」保存复盘，点击「取消」跳过（但仍然会记录交易本身）。
- 勾选了错误分类的交易会 **自动进入错题本**。
- 执行评分会影响会话最终得分的计算。

---

## 4. PA / 方向训练

选择 **「PA / 方向训练」** 模式后，交易操作区隐藏，方向预测区域显示。

### 4.1 方向预测

| 按钮 | 含义 |
|------|------|
| 看涨 | 预测未来 N 根 K 线上涨 |
| 看跌 | 预测未来 N 根 K 线下跌 |
| 震荡 | 预测未来 N 根 K 线横盘 |

- **预测前瞻**：通过「预测前瞻」输入框设置预测未来几根 K 线（默认 3 根）。
- 每次预测后，系统会在 K 线推进后自动验证预测是否正确。
- 训练面板「本轮统计」区域会实时更新预测准确率。

### 4.2 结构标注保存

在 PA 训练中，你可以使用图表左侧的 **绘图工具**（趋势线、通道线、射线、矩形等）在图表上标注价格结构。

**标注保存机制：**
- 点击「结束训练并保存」时，系统会 **自动提取图表上的所有画图标注**。
- 标注会作为 PA Annotation 保存到数据库中，关联本次训练的 Setup、场景、方向等信息。
- 在复盘面板的「训练记录」中可以查看每次训练保存了多少 PA 标注。

**重要提示：** 如果本次 PA 训练你 **没有做任何结构标注**，系统会在错题本中自动生成一条「未做结构标注」的提醒。

### 4.3 剧本训练

剧本训练是 PA 训练的核心理念：在看到 K 线走势前，先写下你的行情推演剧本。

**操作方式：**
1. 在训练前计划卡的「计划/剧本」文本框中，写入你对行情的推演。例如：
   - "预期先下探至前低附近形成 Pin Bar，然后反弹。如果跌破前低则剧本失效。"
   - "区间震荡已久，预期突破上沿后回踩确认，假突破的话反手做空。"
2. 训练过程中，面板「本轮统计」区域会显示你写的剧本，便于随时对照。
3. 结合方向预测 + 图表标注，练习"提前构思 → 验证 → 修正"的思维闭环。

---

## 5. 场景训练

在训练前计划卡中选择场景模板后，系统会尝试从历史数据中 **智能筛选** 符合该场景特征的 K 线片段。

### 可用场景及其筛选规则

| 场景 | 筛选逻辑 |
|------|---------|
| **趋势回踩** | 可见区间涨幅 > 3%，未来区间有回踩（-1.5%）但最终价格不低于可见末端 |
| **区间突破** | 可见区间波动率 < 8%，未来区间有效突破上沿或下沿超过 1% |
| **假突破** | 未来区间先突破上/下沿 1% 以上，但最终价格又回落到区间内 |
| **反转确认** | 可见区间与未来区间走势方向相反，且反转幅度 > 2% |
| **高波动** | 未来区间整体波动率 > 8% |
| **震荡** | 总体涨跌幅 < 3%，未来区间波动率 < 10% |

**注意事项：**
- 场景筛选是 **尽力匹配**：如果找不到符合条件的片段，系统会回退到随机选取。
- 你可以选择「随机」跳过场景筛选，获得完全随机的训练片段。
- 同一场景反复训练有助于深入理解该类行情特征。

---

## 6. 结束训练与自动评分

点击 **「结束训练并保存」** 按钮后，系统执行以下流程：

1. **自动平仓**：如有未平持仓，以当前价格强制平仓，并弹出执行复盘对话框。
2. **提取标注**：从图表中提取所有画图标注。
3. **计算会话分数**：
   - **交易模式**：如果有执行评分，取所有交易的平均执行评分；否则取胜率 × 100。
   - **PA 模式**：取预测准确率 × 100，如果有标注则额外加 10 分（上限 100）。
4. **保存训练记录**：将整个会话（计划、交易、预测、标注）写入数据库。
5. **自动生成错题**：
   - 有错误分类的交易 → 生成交易类错题。
   - 预测准确率 < 50% 或未做标注 → 生成 PA 类错题。
6. **刷新复盘面板**：弹出确认提示后，复盘面板自动刷新。

---

## 7. 复盘面板

复盘面板位于右侧标签页的 **「复盘」** 选项卡中，包含 4 个子标签页。

### 7.1 总览

提供全局训练统计概览。

**筛选功能（顶部）：**
- 品种：按股票代码筛选
- 周期：按 K 线周期筛选（月线 / 日线 / 5分钟 / 1分钟）
- Setup：按 Setup 类型筛选
- 场景：按场景标签筛选

点击「刷新」按钮后重新加载所有统计。

**核心指标区域显示：**
- 训练场次、总交易数
- 胜率（胜/负笔数）
- 平均 R 倍数、盈亏比 (PF)
- 期望值、净盈亏
- 手续费、最大回撤
- 预测次数、预测准确率

**统计钻取区域显示：**
- 最佳 Setup：胜率最高的 Setup 类型
- 最常见错误：出现次数最多的错误分类
- 高频场景：训练次数最多的场景
- 最伤收益出场：平均盈亏百分比最低的出场方式
- 平均执行评分
- PA 标注准确率、错题复训率

**训练建议区域显示：**
- 近 20 笔滚动胜率
- 累计净盈亏
- 针对最常见错误的刻意练习建议

### 7.2 交易日志

以表格形式展示所有交易记录，包含 14 列：

| 列 | 说明 |
|----|------|
| 时间 | 入场时间 |
| 品种 | 股票代码 |
| 周期 | K 线周期 |
| 方向 | 做多 / 做空 |
| Setup | 训练 Setup 类型 |
| 盈亏 | 绝对盈亏金额（红色 = 盈利，绿色 = 亏损） |
| 盈亏% | 盈亏百分比 |
| R倍数 | 以止损为 1R 计算的收益倍数 |
| 出场 | 出场原因（manual / stop_loss / take_profit / session_end） |
| 执行分 | 执行评分（0-100） |
| 错误分类 | 本笔交易的错误标签 |
| 标签 | PA 标签 |
| 入场理由 | 入场说明 |
| 出场复盘 | 出场反思 |

**表格下方为「标签与执行复盘」编辑区：**

1. **快速标签按钮**：8 个常用 PA 标签（Pin Bar、Engulfing、Inside Bar、Outside Bar、Double Top、Double Bottom、Head & Shoulders、Break of Structure），点击即可为选中交易打上/取消标签。
2. **执行评分**：调整选中交易的执行评分。
3. **错误分类**：以逗号分隔输入错误类型。
4. **入场理由**：文本框，编辑入场原因。
5. **出场复盘**：文本框，编辑出场反思。
6. **笔记**：附加笔记。
7. 点击 **「保存选中交易」** 将所有编辑保存到数据库。

**使用场景：** 训练结束后的二次复盘。如果训练时匆忙跳过了执行复盘弹窗，可以在这里补充完善。

### 7.3 训练记录

以表格形式展示所有训练会话，包含 10 列：

| 列 | 说明 |
|----|------|
| 会话ID | 前 8 位会话标识 |
| 品种 | 股票代码 |
| 周期 | K 线周期 |
| 模式 | 交易 / PA |
| Setup | 训练 Setup 类型 |
| 场景 | 场景标签 |
| 开始时间 | 训练开始时间 |
| 交易数 | 该会话内的交易笔数 |
| 净盈亏 | 该会话所有交易净盈亏（红 = 盈 / 绿 = 亏） |
| 分数 | 会话综合评分 |

**双击任意行** 可以展开详细信息，包括：
- 完整训练计划（Setup、场景、方向、失效条件、剧本）
- 交易统计（胜负、净盈亏）
- 平均执行评分
- 预测准确率
- PA 标注数量
- 主要错误分类
- 历史笔记
- 下次训练建议

**会话详情下方** 有「训练总结」文本框和「保存训练笔记」按钮，可以为每次训练写总结反思。

### 7.4 错题本

自动收集训练中的错误，便于针对性复训。

**错题来源：**
- **交易类错题**：平仓时在执行复盘弹窗中勾选了错误分类的交易。
- **PA 类错题**：PA 训练中预测准确率低于 50% 或未做结构标注。

**筛选操作：**
- 顶部下拉框可按类别筛选：全部 / 交易 / PA。
- 点击「刷新错题」重新加载。

**错题表格包含 7 列：** 时间、品种、周期、类别、Setup、错误分类、描述。

**复训标记：**
选中一条错题记录后，点击 **「标记为已复训」** 按钮，系统会将该错题标记为已完成复训。复训率会体现在总览页面的统计钻取中。

---

## 8. 典型训练流程

### 流程一：交易训练完整流程

```
1. 输入代码 → 选择周期 → 点击「下载数据」
2. 选择「模拟交易」模式 → 点击「开始训练」
3. 填写训练前计划卡：
   - Setup: 趋势回踩
   - 场景: 趋势回踩
   - 方向: 看多
   - 失效条件: 跌破前低
   - 单笔风险: 1%
   - 计划: "等回踩均线后出现阳线确认再入场，止损放在回踩最低点"
4. 图表加载 → 观察行情 → 使用画图工具标注关键位置
5. 设置止损价 → 勾选「自动按风险算仓位」→ 点击「市价做多」
6. 推进 K 线观察走势 → 触发止盈/止损或手动平仓
7. 弹出交易执行复盘 → 如实勾选错误分类 → 打分 → 填写复盘
8. 继续交易或点击「结束训练并保存」
9. 切换到「复盘」标签页查看统计 → 在交易日志中补充标签和笔记
10. 查看错题本 → 针对薄弱环节开始下一次刻意练习
```

### 流程二：PA 训练完整流程

```
1. 输入代码 → 选择周期 → 确保已有数据
2. 选择「PA / 方向训练」模式 → 点击「开始训练」
3. 填写训练前计划卡：
   - Setup: 假突破反手
   - 场景: 假突破
   - 方向: 看空
   - 计划/剧本: "预期会先突破上沿，形成假突破后回落。关注是否出现 Pin Bar 或 Engulfing 确认"
4. 图表加载 → 观察可见K线 → 用趋势线/矩形标注区间和关键位
5. 点击「看涨/看跌/震荡」做方向预测（前瞻 3 根K线）
6. 推进 K 线 → 验证预测 → 继续标注新的结构
7. 重复步骤 5-6 直到训练结束
8. 点击「结束训练并保存」→ 系统自动提取标注
9. 查看复盘面板 → PA 标注条数、预测准确率、错题记录
```

---

## 9. 数据结构说明

### 训练记录 (sessions) 新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| setup_type | TEXT | Setup 类型 |
| scenario_tag | TEXT | 场景标签 |
| plan_notes | TEXT | 训练计划/剧本 |
| plan_direction | TEXT | 预期方向 (long/short/sideways) |
| plan_invalidation | TEXT | 失效条件 |
| difficulty | INTEGER | 难度评级 (1-5) |
| score | INTEGER | 会话综合分数 |

### 交易记录 (trades) 新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| mistake_tags | TEXT (JSON) | 错误分类标签数组 |
| execution_score | INTEGER | 执行评分 (0-100) |
| planned_risk_pct | REAL | 计划风险百分比 |
| entry_reason | TEXT | 入场理由 |
| exit_review | TEXT | 出场复盘 |
| commission | REAL | 手续费 |

### PA 标注表 (pa_annotations)

| 字段 | 类型 | 说明 |
|------|------|------|
| session_id | TEXT | 所属训练会话 |
| ann_type | TEXT | 标注类型 (drawings) |
| data_json | TEXT | 标注数据 JSON |
| notes | TEXT | 备注 |
| is_correct | INTEGER | 是否正确 (预留) |

### 错题本 (mistake_book)

| 字段 | 类型 | 说明 |
|------|------|------|
| session_id | TEXT | 所属训练会话 |
| symbol | TEXT | 品种代码 |
| timeframe | TEXT | K 线周期 |
| category | TEXT | 类别 (trade / pa) |
| setup_type | TEXT | Setup 类型 |
| mistake_tags | TEXT (JSON) | 错误标签数组 |
| description | TEXT | 错误描述 |
| slice_start | INTEGER | K 线片段起始索引 |
| slice_end | INTEGER | K 线片段结束索引 |
| retrained | INTEGER | 是否已复训 (0/1) |

---

## 10. 常见问题

**Q: 计划卡可以跳过吗？**
A: 不可以。点击计划卡对话框的「取消」按钮会放弃本次训练。这是设计意图——养成"先计划再交易"的习惯。

**Q: 交易执行复盘弹窗可以跳过吗？**
A: 可以。点击「取消」会跳过复盘，但交易本身仍会记录。不过建议每次都认真填写，这是提升执行质量的关键。

**Q: 场景筛选总是命中吗？**
A: 不一定。如果历史数据中没有符合条件的片段，系统会自动回退到随机选取，并正常开始训练。

**Q: A 股可以做空吗？**
A: 默认不可以。系统遵循 A 股规则，「市价做空」和「限价卖出」按钮会被禁用。

**Q: 「自动按风险算仓位」和「计算仓位」按钮有什么区别？**
A: 勾选「自动」后，每次点击做多/做空按钮时会自动计算并下单。「计算仓位」按钮是手动触发计算，只填入数量但不下单，方便你确认后再操作。

**Q: 错题本的错题是怎么产生的？**
A: 两种来源：① 交易平仓复盘时勾选了至少一个错误分类 → 自动生成交易类错题；② PA 训练中预测准确率低于 50% 或未做标注 → 自动生成 PA 类错题。

**Q: 如何做刻意练习？**
A: 在错题本中找到你最频繁的错误类型（如"拖延止损"），然后开始新训练时在计划卡中选择与该错误相关的 Setup 和场景，集中练习同一类行情。训练建议区域也会给出提示。

**Q: 数据保存在哪里？**
A: K 线数据以 SQLite 文件形式保存在 `~/.priceaction/` 目录下。训练记录（sessions、trades、predictions、pa_annotations、mistake_book）保存在同一目录的数据库中。
